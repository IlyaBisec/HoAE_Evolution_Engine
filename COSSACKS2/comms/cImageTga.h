#pragma once
/*
class cImageTga : public cImageCodec {
public:
	cImageTga() : cImageCodec() {}
	virtual cImage * Decode(const cData *Src);
private:
	bool LoadRgb8(const cData *Src, cImage *Tex, dword *Buffer, const dword *Palette, bool VertInvert);
	bool LoadRgb15(const cData *Src, cImage *Tex, dword *Buffer, bool VertInvert);
	bool LoadRgb24(const cData *Src, cImage *Tex, dword *Buffer, bool VertInvert);
	bool LoadRgb32(const cData *Src, cImage *Tex, dword *Buffer, bool VertInvert);
	bool LoadRle8(const cData *Src, cImage *Tex, dword *Buffer, const dword *Palette, bool VertInvert);
	bool LoadRle15(const cData *Src, cImage *Tex, dword *Buffer, bool VertInvert);
	bool LoadRle24(const cData *Src, cImage *Tex, dword *Buffer, bool VertInvert);
	bool LoadRle32(const cData *Src, cImage *Tex, dword *Buffer, bool VertInvert);
};*/